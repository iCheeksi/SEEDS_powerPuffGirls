{
    "scripts": {
       
        "start": "cd registerbackend && npm install && npm start",
        "heroku-postbuild": "cd register && npm install && npm run build"
        
    },
    "dependencies": {
        "react-router-dom": "^5.2.0"
    }
}
